<div class="page-header">
  <h1><%=t '.title', default: Form.model_name.human %></h1>
</div>

<p>
  <p>
    <strong><%= Form.human_attribute_name(:title) %>:</strong>
    <%= @form.title %>
  </p>
  <p>
    <strong><%= Form.human_attribute_name(:enabled) %>:</strong>
    <%= @form.enabled %>
  </p>
  <p>
    <strong><%= Form.human_attribute_name(:description) %>:</strong>
    <%= @form.description %>
  </p>
</p>
<%= render 'form_filled_forms', form: @form %>

<div class="form-actions">
  <%= fill_new_btn @form %>
  <%= link_to t('.back', default: t("helpers.links.back")),
    forms_path, :class => 'btn'  %>
</div>
